# =============================================================================
# Camelot v3 LP Rebalancing Agent - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# AI Provider Configuration (Required - choose at least one)
# -----------------------------------------------------------------------------

# OpenRouter (Recommended - supports multiple models)
OPENROUTER_API_KEY=your-openrouter-api-key

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# Anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key

# xAI (Grok)
XAI_API_KEY=your-xai-api-key

# Hyperbolic
HYPERBOLIC_API_KEY=your-hyperbolic-api-key

# -----------------------------------------------------------------------------
# AI Model Selection (Optional)
# -----------------------------------------------------------------------------

# Preferred AI provider (defaults to first available)
# Options: openrouter | openai | anthropic | xai | hyperbolic
AI_PROVIDER=openrouter

# Specific model to use (defaults to provider-specific model)
# OpenRouter examples: google/gemini-2.5-flash, anthropic/claude-3.5-sonnet
# OpenAI examples: gpt-4o, gpt-4o-mini
# Anthropic examples: claude-3-5-sonnet-20241022, claude-3-5-haiku-20241022
AI_MODEL=google/gemini-2.5-flash

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------

# Arbitrum RPC URL (Required for on-chain operations)
ARBITRUM_RPC_URL=https://arb1.arbitrum.io/rpc

# Ember MCP Server URL (Required for Camelot v3 operations)
EMBER_MCP_SERVER_URL=https://api.emberai.xyz/mcp

# Enhanced RPC access (Optional - for better performance)
QUICKNODE_SUBDOMAIN=your-quicknode-subdomain
QUICKNODE_API_KEY=your-quicknode-api-key

# -----------------------------------------------------------------------------
# Agent Configuration (Optional)
# -----------------------------------------------------------------------------

# Agent server configuration
PORT=3001
ENABLE_CORS=true
BASE_PATH=/

# Agent metadata (Optional overrides)
AGENT_NAME=Camelot v3 LP Rebalancer
AGENT_VERSION=1.0.0
AGENT_DESCRIPTION=Automated liquidity management agent for Camelot v3 concentrated liquidity pools with risk-based rebalancing strategies
AGENT_URL=localhost

# -----------------------------------------------------------------------------
# Wallet Configuration (For Active Mode - KEEP SECURE!)
# -----------------------------------------------------------------------------

# Private key for transaction signing (Active Mode only)
# WARNING: Keep this secure! Never commit to version control!
# Use environment variables or secure key management in production
PRIVATE_KEY=your-wallet-private-key-here

# Wallet address (derived from private key, used for position queries)
WALLET_ADDRESS=your-wallet-address-here

# -----------------------------------------------------------------------------
# Rebalancing Strategy Configuration (Optional)
# -----------------------------------------------------------------------------

# Default risk profile (low | medium | high)
DEFAULT_RISK_PROFILE=medium

# Default operating mode (active | passive)
DEFAULT_OPERATING_MODE=passive

# Default monitoring interval in seconds
DEFAULT_MONITORING_INTERVAL=300

# Minimum USD value to consider for rebalancing
MIN_LIQUIDITY_VALUE=100

# Maximum slippage tolerance (as percentage, e.g., 1.0 = 1%)
MAX_SLIPPAGE=1.0

# -----------------------------------------------------------------------------
# Monitoring & Alerts Configuration (Optional)
# -----------------------------------------------------------------------------

# Notification method for passive mode (console | webhook | email)
NOTIFICATION_METHOD=console

# Webhook URL for notifications (if using webhook)
WEBHOOK_URL=https://your-webhook-url.com/notify

# Email configuration (if using email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
NOTIFICATION_EMAIL=alerts@yourdomain.com

# -----------------------------------------------------------------------------
# Logging & Debugging (Optional)
# -----------------------------------------------------------------------------

# Log level (error | warn | info | debug)
LOG_LEVEL=info

# Enable debug logging for specific components
DEBUG=camelot-rebalancer:*

# Log file path (optional - logs to console by default)
LOG_FILE=./logs/rebalancer.log

# -----------------------------------------------------------------------------
# Database Configuration (Optional - for persistent storage)
# -----------------------------------------------------------------------------

# Database URL for storing configs and performance data
# Example: postgresql://user:pass@localhost:5432/rebalancer
DATABASE_URL=

# Redis URL for caching and session storage
# Example: redis://localhost:6379
REDIS_URL=

# -----------------------------------------------------------------------------
# Security & Production Settings (Optional)
# -----------------------------------------------------------------------------

# API rate limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=900000

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Enable/disable specific features
ENABLE_METRICS=true
ENABLE_HEALTH_CHECKS=true
ENABLE_SWAGGER_DOCS=false

# -----------------------------------------------------------------------------
# Development Settings (Optional)
# -----------------------------------------------------------------------------

# Enable development features
NODE_ENV=production

# Mock mode (for testing without real transactions)
MOCK_MODE=false

# Testnet mode (use testnet instead of mainnet)
TESTNET_MODE=false

# -----------------------------------------------------------------------------
# Integration Settings (Optional)
# -----------------------------------------------------------------------------

# External price feed API keys (for enhanced price data)
COINGECKO_API_KEY=your-coingecko-api-key
CHAINLINK_API_KEY=your-chainlink-api-key

# Analytics and monitoring services
DATADOG_API_KEY=your-datadog-api-key
SENTRY_DSN=your-sentry-dsn

# =============================================================================
# Setup Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in at least one AI provider API key
# 3. Configure ARBITRUM_RPC_URL and EMBER_MCP_SERVER_URL
# 4. For Active Mode: Add PRIVATE_KEY and WALLET_ADDRESS
# 5. Customize other settings as needed
# 6. Never commit .env file to version control!
# =============================================================================
