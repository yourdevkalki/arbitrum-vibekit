services:
  app:
    build:
      context: ../..
      dockerfile: lib/agent-node/Dockerfile
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./config:/app/config:ro
